DATABASE_URL=file:./cms.db
PAYLOAD_SECRET=YOUR_SECRET_HERE_GENERATE_WITH_OPENSSL_RAND_HEX_32
UPLOADS_DIR=./media

# Używane przez `apps/cms/scripts/seed-admin.ts` (po reset/fresh dev DB).
# Domyślne w skrypcie (gdy brak zmiennych): admin@wks.local / admin — TYLKO DEV.
# (`admin@admin` odrzuca walidacja Payload — trzeba domeny w stylu .local / .test)
ADMIN_EMAIL=admin@wks.local
ADMIN_PASSWORD=admin
